<%= form_tag(boards_path, method: :get) do %>
    <%= text_field_tag :rt_num, params[:rt_num] %>
    <%= submit_tag 'Search'%>
<% end %>

<%= @result%>
<%=  @name %>


    <div class="form-group row">
        <div class="col-md-4 col-md-offset-3">
            <input type="text" name="rt_num" class="form-control text-center" placeholder="검색어">
        </div>
        <div class="col-md-2">
              <button type="submit" class="btn btn-success form-control"><a href='/result/<%=@rt_num%>'>검색하기</button>
        </div>
    </div>

  <%# <%= link_to '에 대한 다음(kakao) 검색 결과 확인하기', "/result/"+@rt_num%> 
  