<%# <marquee>공지사항입니다</marquee>  %>
<h1>걱정없식</h1>
<h2>검색하기</h2>

<%= link_to '알레르기 검색', menus_search_path %><br>
<%= link_to '식당으로 검색', restaurants_search_path%><br>

<div class="container">
  <h1 style = "margin-top: 40px; text-align: center"> HOME </h1>
  <h1 style = "margin-top: 40px; text-align: center"> </h1>
  <div class="form-group" style="margin-top: 50px; text-align: center">
    <% if user_signed_in? %>
      <p> 현재 로그인된 유저의 이름: <%= current_user.name %> </p>    
      <p> 현재 로그인된 유저의 이메일: <%= current_user.email %> </p>
      <p> 현재 로그인된 유저의 성별: <%= current_user.gender %> </p>
      <p> 현재 로그인된 유저의 연령대: <%= current_user.ages %> </p>      
      <p> 현재 로그인된 유저의 알러지정보: <%= current_user.allergy.gsub(',','') %> </p>      
      <p><%= link_to('정보수정', edit_user_registration_path) %></p>
      <%# <p><%= link_to('정보업데이트', editsns_path) %>
    <%end%>
</div>

<!-- 수영 임시코드 -- >
<%# <a onclick="showHide('info_html')" onfocus="this.blur()" style="color:white;margin-top:7px;"><b> 알림 (<%= current_user.new_alarms.unread_by(current_user).count %>)</b></a> %>
<%# <div id="info_html" style="display:none; margin-top: 10px;"> %>
<%# <%= render partial: "new_alarms/notice_alarm", collection: current_user.new_alarms.unread_by(current_user) %>
<%# <%= link_to '<b>X</b>'.html_safe, new_alarms_read_all_path %> 
<%# </div> %>

<%# if $articles_num == $articles_num + 1 %>
<%# <%="테스트"%> 
<%# end %>


<!--공지 팝업창 수정 필요 -->
<div id="myModal" class="modal">
  <div class="modal-content">
    <span class="close">&times;</span>
    <h4>새로운 식당이 업데이트 되었습니다.</h4><br/>
      <h6>8월22일자: </h6>
    <button id="no">오늘하루 보지않기</button>
  </div>
</div>

<style> 
/* 임시 ***************/
#no{
  width:200px;
  margin-left:38%;
}
/***********************/
.modal {
    display: none; /* Hidden by default */
    position: fixed; /* Stay in place */
    z-index: 1; /* Sit on top */
    padding-top: 100px; /* Location of the box */
    overflow: auto; /* Enable scroll if needed */
}

.modal-content {
    background-color: #fefefe;
    margin: auto;
    padding: 20px;
    border: 1px solid #888;
    width: 30%;
    height: 30%;
    margin-right:100px;
    margin-left:20px;
}

/* The Close Button */
.close {
    text-align: right;
    color: #aaaaaa;
    float: right;
    font-size: 28px;
    font-weight: bold;
}

.close:hover,
.close:focus {
    color: #000;
    text-decoration: none;
    cursor: pointer;
}
</style>

<% if @boadrs_num != 0  %>
<script>
  var modal = document.getElementById('myModal');
  var span = document.getElementsByClassName("close")[0];
  var later = document.getElementById("no")
  span.onclick = function() {
          modal.style.display = "none";   
  }
  later.onclick = function() {
        modal.style.display = "none";
  }    
  // page 로드시 팝업창  
  function fnShowPopup() {
      // alert('');
      modal.style.display = "block"; 
  }
  $(document).ready(function () {
      setTimeout(fnShowPopup, 300); 
  });
  
</script>
<% end  %>




